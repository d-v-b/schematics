[workspace]
name = "beam-clip"
channels = ["conda-forge"]
platforms = ["osx-arm64", "osx-64", "linux-64"]

[dependencies]
python = ">=3.12"
build123d = ">=0.9"
just = "*"
pytest = "*"

[tasks]
clip = "just clip"
render = "just render"
preview = "just preview"
test = "just test"
clean = "just clean"
