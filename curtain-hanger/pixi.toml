[workspace]
name = "curtain-hanger"
channels = ["conda-forge"]
platforms = ["osx-arm64", "osx-64", "linux-64"]

[dependencies]
python = ">=3.12"
build123d = ">=0.9"
just = "*"
pytest = "*"

[tasks]
proto = "just proto"
full = "just full"
all = "just all"
wrap-coupon = "just wrap-coupon"
wrap = "just wrap"
bolt-coupon = "just bolt-coupon"
bolt = "just bolt"
flex-coupon = "just flex-coupon"
flex = "just flex"
rail-coupons = "just rail-coupons"
rail-clamp = "just rail-clamp"
test-run = "just test-run"
preview = "just preview"
test = "just test"
assembly = "just assembly"
clean = "just clean"
