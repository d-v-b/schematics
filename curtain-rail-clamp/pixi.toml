[workspace]
name = "curtain-rail-clamp"
channels = ["conda-forge"]
platforms = ["osx-arm64", "osx-64", "linux-64"]

[dependencies]
python = ">=3.12"
build123d = ">=0.9"
just = "*"
pytest = "*"

[tasks]
clamp = "just clamp"
sweep = "just sweep"
preview = "just preview"
test = "just test"
clean = "just clean"
